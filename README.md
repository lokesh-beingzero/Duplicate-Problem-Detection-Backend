# Duplicate-Problem-Detection-Backend

Install the dependencies

fastapi 
uvicorn 
lancedb
google-genai 
sentence-transformers 
pydantic 

using the following command

`pip install fastapi uvicorn chromadb google-genai sentence-transformers pydantic`

After installing the dependencies, run the project using the following command

`uvicorn app:app`
